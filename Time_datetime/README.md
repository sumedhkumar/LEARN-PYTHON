# Time_datetime Folder

This folder contains Jupyter notebooks related to time and datetime operations in Python.

## Files:

*   **time_datetime.ipynb**: This notebook likely covers the fundamentals of using the `time` and `datetime` modules in Python, including creating and manipulating date and time objects.
*   **timedelta_in_python.ipynb**: This notebook probably focuses on `timedelta` objects, which are used to represent differences between datetimes and are useful for date arithmetic.
